<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.pos.cacaestudio">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".PagamentoActivity"
            android:label="@string/tela_pagamento">
        </activity>

        <activity
            android:name=".AgendadosActivity"
            android:label="@string/tela_agendados">

        </activity>

        <activity
            android:name=".AvaliarActivity"
            android:label="@string/tela_avaliar">

        </activity>

        <activity
            android:name=".ListaActivity"
            android:label="@string/tela_lista"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>